name: 🎯 Análise de Pontuação - Consultores

on:
  workflow_dispatch:
    inputs:
      planilha_cadastros:
        description: '📋 Arraste ou selecione a planilha de CADASTROS (todos os consultores do site)'
        required: true
        type: string
      planilha_pontuacao:
        description: '🏆 Arraste ou selecione a planilha de PONTUAÇÃO do mês (quem pontuou)'
        required: true
        type: string

jobs:
  analisar:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install pandas openpyxl rapidfuzz unidecode

      - name: 📥 Copiar planilhas enviadas
        run: |
          mkdir -p inputs/{cadastros,pontuacao,outputs}
          
          # Copia direto do input (GitHub converte arquivo em caminho)
          cp "${{ inputs.planilha_cadastros }}" inputs/cadastros/
          cp "${{ inputs.planilha_pontuacao }}" inputs/pontuacao/
          
          echo "📁 Arquivos copiados:"
          echo "Cadastros:"
          ls -la inputs/cadastros/
          echo "Pontuação:"
          ls -la inputs/pontuacao/

      - name: 🔍 Executar análise
        run: python analise_pontuacao.py

      - name: 📤 Upload resultado
        uses: actions/upload-artifact@v4
        with:
          name: 🎯-resultado-pontuacao-${{ github.run_number }}
          path: outputs/resultado_*.xlsx
          retention-days: 30

      - name: 📊 Resumo final
        if: always()
        run: |
          echo ""
          echo "✅ Análise pronta! Baixe o Excel nos Artifacts acima."
          echo "Abra a aba 'Resumo' pra ver % de quem pontuou e médias."
